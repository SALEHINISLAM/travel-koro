import React from 'react';
import PropTypes from 'prop-types';

const LogOut = props => {
    return (
        <div>
            logout
        </div>
    );
};

LogOut.propTypes = {
    
};

export default LogOut;