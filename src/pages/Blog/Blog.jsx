import React from 'react';
import PropTypes from 'prop-types';

const Blog = props => {
    return (
        <div>
            blog
        </div>
    );
};

Blog.propTypes = {
    
};

export default Blog;